/* eslint-env jest */

const request = require('supertest');

module.export = (app) => {
  describe('Reviews and Rating Routes API', () => {
    it('should receive an object on GET/reviews with query: {page: 1, count: 5}', async)
  });
};
